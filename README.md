# Deep_Packet_Inspection
Deep Packet Inspection engine in C++ that classifies and filters network traffic by parsing raw PCAP captures across Ethernet/IP/TCP/TLS protocol stacks. Extracts HTTPS domain names via TLS SNI without decryption. Features stateful flow tracking and a multi-threaded LB→FP pipeline for scalable, rule-based packet blocking.
